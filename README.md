# Задание к занятию "Внешние отчеты, обработки и расширения"

## Задача 1. Внешняя обработка «Калькулятор»

### Что сделано:
- Создана внешняя обработка с именем `Калькулятор`.
- На форме добавлены два реквизита типа `Число`: `Значение` и `Результат`.
- На форму вынесены два поля ввода.
- Добавлены 4 команды: `Сложить`, `Вычесть`, `Разделить`, `Умножить`.
- Команды вынесены на форму как кнопки.
- Для каждой команды создан обработчик `&НаКлиенте`.
- В обработчиках реализованы арифметические действия:
  - `Результат = Результат + Значение`
  - `Результат = Результат - Значение`
  - `Результат = Результат / Значение`
  - `Результат = Результат * Значение`
- Обработка сохранена в файл `Калькулятор.epf`.

### Скриншот формы обработки:
> (вставь своё фото)

### Файл обработки:
[Скачать Калькулятор.epf](Калькулятор.epf)

---

## Задача 2. Внешний отчёт «Лекции»

### Что сделано:
- Создан внешний отчёт с именем `Лекции`.
- Добавлена основная схема компоновки данных.
- Создан набор данных — запрос к справочнику `Лекции`.
- В запросе выбраны поля: `Наименование`, `Дата`, `Модуль`.
- В настройках СКД добавлены **детальные записи**.
- В выбранные поля добавлены: `Наименование`, `Дата`, `Модуль`.
- Отчёт сохранён в файл `Лекции.erf`.

### Скриншот схемы компоновки данных и настроек:
> (вставь своё фото)

### Файл отчёта:
[Скачать Лекции.erf](Лекции.erf)

---

## Результат
✅ Оба файла предоставлены  
✅ Калькулятор работает: 4 операции, поля и кнопки на месте  
✅ Отчёт выводит все лекции с нужными полями

Задание сдано.
